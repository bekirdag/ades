# ADES Source Lane and Graph Artifact Inventory

Date: 2026-06-30

Work item: NSC-0003, "Inventory existing ADES source lanes and graph artifacts."

Scope: current ADES source-lane builders, source snapshot fetchers, normalized pack
bundles, and graph artifact builders that can affect BDYA news story creation.
This is a local inventory only; no artifact build or deployment was run.

## Lane Matrix

| Lane name | Input files | Build command | Validation command | Output artifact | Manifest support | Source-tier policy |
| --- | --- | --- | --- | --- | --- | --- |
| Finance raw source snapshot | SEC `company_tickers.json`, SEC submissions ZIP, SEC companyfacts ZIP, Nasdaq symbol directory, Nasdaq other-listed file, optional finance people source | `ades registry fetch-finance-sources` | `pytest tests/unit/test_finance_source_fetch.py tests/component/test_cli_finance_source_fetch.py` | Immutable raw snapshot directory under `/mnt/githubActions/ades_big_data/pack_sources/raw/finance-en` | Writes `sources.fetch.json` through `source_manifest_path` | Raw-source lane records source files; market-graph source-tier promotion is not applied until data is converted into graph edge lanes. |
| Finance normalized bundle | Finance raw snapshot files plus curated finance entities | `ades registry build-finance-bundle` | `ades registry validate-finance-quality` and `pytest tests/unit/test_finance_bundle_builder.py tests/component/test_cli_finance_pack_quality.py` | Normalized `finance-en` bundle directory with matcher inputs | Writes `bundle.json` and source lock metadata | Pack tier is bundle metadata (`domain`); source-tier promotion is not used for matcher-only aliases. |
| Finance-country raw source snapshots | Official country-finance landing pages from configured country profiles | `ades registry fetch-finance-country-sources` | `pytest tests/unit/test_finance_country.py tests/component/test_cli_finance_country.py` | Immutable country snapshot directories under `/mnt/githubActions/ades_big_data/pack_sources/raw/finance-country-en` | Each country snapshot writes `sources.fetch.json` | Raw-source lane records official source URLs; source-tier promotion is applied later when graph rows are generated. |
| Finance-country normalized bundles | Finance-country snapshot directories | `ades registry build-finance-country-bundles` | `pytest tests/unit/test_finance_country.py tests/component/test_cli_finance_country.py` | Country pack bundle directories under `/mnt/githubActions/ades_big_data/pack_sources/bundles/finance-country-en` | Each bundle writes `bundle.json` | Pack tier is bundle metadata (`domain`); graph source-tier policy is deferred to proxy/input graph lanes. |
| Finance-country source-lane inputs | Installed `finance-*-en` pack directories with `bundle.json`, entities, and `sources.json` or `build.json` metadata | `ades registry derive-finance-country-source-lane-inputs` | `pytest tests/unit/test_finance_country_source_lane_inputs.py` | `issuer_sector.tsv` and `issuer_geography.tsv` under the run ID subdirectory of `/mnt/githubActions/ades_big_data/pack_sources/impact_relationships/finance_country_source_lane_inputs` | Writes `manifest.json` with row counts and source input paths | Uses pack metadata as local source URLs where needed; downstream graph validation classifies these as local pack metadata unless official URLs are carried forward. |
| Finance-country proxy source lane | Installed `finance-*-en` pack directories, plus optional extra proxy pack IDs | `ades registry build-finance-country-proxy-source-lane` | `ades registry validate-market-graph-source-lanes` with `--edge-tsv-path` set to the generated `impact_edges.tsv`, and `pytest tests/unit/test_finance_country_proxy_impact.py tests/component/test_cli_market_graph_store.py` | Normalized market graph node/edge TSV lane; optional `market_graph_store.sqlite` when `--build-artifact` is supplied | Writes lane `manifest.json`; optional graph build writes `market_graph_store_manifest.json` | Graph validation calls `classify_source_tier()` and `validate_source_attribution()`; promotion is safest for official/high-trust pack-derived sources and warnings are emitted for unknown/test/Wikidata-only sources. |
| General raw source snapshot | Wikidata truthy RDF dump, Wikidata entity JSON dump or prefiltered snapshot, GeoNames allCountries and alternate names, optional GeoNames deltas | `ades registry fetch-general-sources` | `pytest tests/unit/test_general_source_fetch.py tests/component/test_cli_general_source_fetch.py` | Immutable raw snapshot directory under `/mnt/githubActions/ades_big_data/pack_sources/raw/general-en` | Writes `sources.fetch.json` | Raw-source lane records source files; source-tier promotion is not applied until graph edge lanes are built. |
| General normalized bundle | Wikidata entity snapshot, GeoNames places snapshot, curated general entities | `ades registry build-general-bundle` | `ades registry validate-general-quality` and `pytest tests/unit/test_general_bundle_builder.py tests/component/test_cli_general_pack_quality.py` | Normalized `general-en` bundle directory | Writes `bundle.json` and source lock metadata | Pack tier is bundle metadata; matcher aliases are not production market-graph edges. |
| Medical raw source snapshot | Disease Ontology OBO, HGNC genes JSON, UniProt reviewed-protein JSON, ClinicalTrials.gov studies JSON, FDA Orange Book products | `ades registry fetch-medical-sources` | `pytest tests/unit/test_medical_source_fetch.py tests/component/test_cli_medical_source_fetch.py` | Immutable raw snapshot directory under `/mnt/githubActions/ades_big_data/pack_sources/raw/medical-en` | Writes `sources.fetch.json` | Raw-source lane records source files; source-tier promotion is not applied until graph edge lanes are built. |
| Medical normalized bundle | Disease ontology, HGNC, UniProt, ClinicalTrials, optional Orange Book products, curated medical entities | `ades registry build-medical-bundle` | `ades registry validate-medical-quality` and `pytest tests/unit/test_medical_bundle_builder.py tests/component/test_cli_medical_pack_quality.py` | Normalized `medical-en` bundle directory | Writes `bundle.json` and source lock metadata | Pack tier is bundle metadata; matcher aliases are not production market-graph edges. |
| Policy-sector source lane | Policy/regulator/law-to-sector TSVs plus issuer-sector TSVs and optional extra graph TSVs | `ades registry build-policy-sector-source-lane` | `ades registry validate-market-graph-source-lanes` with `--edge-tsv-path` set to the generated `impact_edges.tsv`, and `pytest tests/unit/test_policy_sector_proxy_impact.py tests/component/test_cli_market_graph_store.py` | Normalized `impact_nodes.tsv` and `impact_edges.tsv`; optional `market_graph_store.sqlite` | Writes lane `manifest.json`; optional graph build writes `market_graph_store_manifest.json` | Production rows are checked by `source_catalog.py`; government, exchange, regulator, issuer-disclosed, licensed, industry-association, reviewed-proposal, and local-pack tiers are classified from source name/URL. Unknown, test fixture, and Wikidata-only bridge sources warn. |
| Program/org relationship source lane | Program, product, brand, organization, holding, issuer, security, and ticker relationship TSVs | `ades registry build-program-org-relationship-source-lane` | `ades registry validate-market-graph-source-lanes` with `--edge-tsv-path` set to the generated `impact_edges.tsv`, and `pytest tests/unit/test_program_org_relationship_impact.py tests/component/test_cli_market_graph_store.py` | Normalized `impact_nodes.tsv` and `impact_edges.tsv`; optional `market_graph_store.sqlite` | Writes lane `manifest.json`; optional graph build writes `market_graph_store_manifest.json` | Same `source_catalog.py` source-tier and attribution warnings as other market graph edge lanes; row relation metadata is also checked by `relationship_schema.py`. |
| Issuer-exposure source lane | Issuer-to-commodity, issuer-to-geography, and issuer supply-chain/counterparty TSVs | `ades registry build-issuer-exposure-source-lane` | `ades registry validate-market-graph-source-lanes` with `--edge-tsv-path` set to the generated `impact_edges.tsv`, and `pytest tests/unit/test_issuer_exposure_impact.py tests/component/test_cli_market_graph_store.py` | Normalized `impact_nodes.tsv` and `impact_edges.tsv`; optional `market_graph_store.sqlite` | Writes lane `manifest.json`; optional graph build writes `market_graph_store_manifest.json` | Same `source_catalog.py` source-tier and attribution warnings as other market graph edge lanes; incomplete or weakly sourced exposure rows should stay out of production artifacts. |
| BDYA impact-gap review import | BDYA impact-gap backlog JSONL exports | `ades registry import-bdya-impact-gap-backlog` | `pytest tests/unit/test_bdya_gap_backlog_import.py` | ADES review queue JSONL files under the run ID subdirectory of `/mnt/githubActions/ades_big_data/pack_sources/impact_relationships/bdya_gap_backlog` | Writes import summary/manifest outputs through the import result | This is proposal/review input only. News-derived claims are not source-tier eligible production graph truth until reviewed and promoted with supporting source attribution. |
| Reviewed relationship proposal promotion | Reviewed proposal JSONL files | `ades registry promote-reviewed-relationship-proposals` | `pytest tests/unit/test_proposal_promoter.py` plus `ades registry validate-market-graph-source-lanes` with `--edge-tsv-path` set to the promoted `impact_edges.tsv` | Accepted graph edge TSV rows and rejected/review outputs | Writes promotion outputs under the configured output root | Reviewed proposals are classified as `reviewed_proposal`; they still require source attribution validation and may need stronger supporting sources before production promotion. |
| Packaged Phase 1 starter market lane | `src/ades/resources/impact/phase1_starter/impact_nodes.tsv`, `impact_edges.tsv`, and `source_manifest.json` | `ades registry build-starter-market-graph-store` | `pytest tests/unit/test_impact_starter_evaluation.py tests/unit/test_source_lane_validation.py` | Starter `market_graph_store.sqlite` | Packaged `source_manifest.json`; graph build writes `market_graph_store_manifest.json` | Manifest says raw/bulk source data belongs under `/mnt/githubActions/ades_big_data`; graph build/validation reports `source_tier_counts`, source warnings, and relation warnings. |
| Generic market graph store artifact | One or more normalized market graph edge TSVs plus optional node TSVs | `ades registry build-market-graph-store` | `ades registry validate-market-graph-source-lanes` with `--edge-tsv-path` set to each normalized edge lane, and release gate commands supplied via `--release-gate-command` | `market_graph_store.sqlite` | Writes `market_graph_store_manifest.json` with artifact hash, source manifest hash, row counts, relation/family counts, source-tier counts, warnings, and release-gate result | Build and validation both use source-tier classification from source name/URL and relation metadata validation; warnings do not automatically fail unless the caller's release gate enforces failure. |
| QID graph store artifact | Normalized bundle directories with `bundle.json` plus Wikidata truthy RDF dump | `ades registry build-qid-graph-store` | Release gate commands supplied via `--release-gate-command`; unit/component coverage in `tests/unit/test_vector_graph_store.py` and `tests/component/test_cli_qid_graph_store.py` | Explicit QID graph SQLite store | Writes graph-store manifest through the vector graph builder | Not a market impact source-tier lane. Source trust is inherited from source bundles used to generate pack entities. |
| QID graph vector index artifact | Normalized bundle directories plus Wikidata truthy RDF dump, or existing QID graph store | `ades registry build-qid-graph-index` or `ades registry build-qid-graph-index-from-store` | Release gate commands supplied via `--release-gate-command`; vector quality can be checked with `ades registry evaluate-vector-quality` | Hosted/local QID graph-vector artifact; optional Qdrant collection/alias when publish options are supplied | Writes QID graph artifact manifest and optional Qdrant publication metadata | Not a market impact source-tier lane. Source trust is inherited from source bundles and graph-store inputs. |
| Unified artifact release manifest | Artifact descriptors or JSON spec for packs, market graph, QID, Qdrant, and recent-news artifacts | `ades registry write-artifact-release-manifest` | Required artifact kinds via `--require-kind` and release gate commands via `--release-gate-command` | Release manifest JSON | Writes the requested manifest path | Governance layer only. It records artifact provenance but does not classify source tiers itself. |

## Source-Tier Policy Summary

Market impact edge lanes share one policy surface:

- `src/ades/impact/source_catalog.py` classifies source tiers from `source_name` and `source_url`.
- Promotion-friendly tiers include official/high-trust source families such as government, regulator, exchange, issuer-disclosed, licensed, industry-association, reviewed-proposal, and local pack metadata.
- `validate_source_attribution()` warns on missing source fields, unknown source tiers, Wikidata-only bridge sources, and test fixtures.
- `src/ades/impact/source_lane_validation.py` reports `source_tier_counts`, `source_warning_counts`, and `relation_warning_counts` for edge TSV lanes.
- `src/ades/impact/graph_builder.py` carries the same counts and warnings into `market_graph_store_manifest.json`.

Important gap for follow-up work: current market graph edge TSVs do not carry an
explicit `source_tier` column. Tier is inferred at validation/build time from
source name and URL, so future production promotion rules should either persist
the inferred tier or add an explicit reviewed tier field before release gating.
